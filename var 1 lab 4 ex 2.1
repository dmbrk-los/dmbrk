#include <stdio.h>
#include <math.h>
int main()
{
int x,a=0;
scanf ("%d",&x);
if (x<=10)
{
    x=10;
}
if (x>100)
{
    printf ("число х занадто велике");
    return 0;
}
for (int r=x; r<=100; r++)
{
    if (r%2==0)
    {
    a+=r;
    }
}
printf ("%d", a);
}
