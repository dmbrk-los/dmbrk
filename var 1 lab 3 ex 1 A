#include <stdio.h>
#include <math.h>
int main()
{
float x,f;
printf("введіть: x\n");
scanf("%f",&x);
if (x>0)
{
   f=pow(x,2)+5*x-6;
}
if (x<=0)
{
    f=fabs(x+cos(x));
}
printf("відповідь: ");
printf("%f",f);
}
