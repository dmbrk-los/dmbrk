#include <stdio.h>
#include <math.h>
int main()
{
int n;
float x, s;
scanf ("%d",&n);
scanf ("%f",&x);
for (int i=1;i<n;i++)
    {
    for (int j=i;j<n;j++)
        {
        s+=(x+i)/j;
        }
    }
printf ("%f", s);
}
